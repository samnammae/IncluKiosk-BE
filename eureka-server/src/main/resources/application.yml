server:
  port: 8761 # 유레카 서버의 포트 번호입니다. 관례적으로 8761을 사용합니다.

eureka:
  client:
    register-with-eureka: false # 자신을 유레카 서버에 등록하지 않도록 설정합니다.
    fetch-registry: false       # 레지스트리 정보를 로컬에 캐싱하지 않도록 설정합니다.
  server:
    wait-time-in-ms-when-sync-empty: 0 # 초기 동기화 대기 시간을 0으로 설정하여 빠른 시작을 돕습니다.